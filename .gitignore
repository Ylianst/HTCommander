/reference
/references

# Release binaries: keep out of git to avoid history bloat (host them on GitHub Releases).
# The small release.json feed files under docs/releases are still tracked.
/releases/
docs/releases/**/*.zip

# Crash reports exported from Xcode Organizer (dropped in the workspace root for
# investigation). Never commit these.
/*.xccrashpoint/
/*.crash
/*.ips

# App Store Connect API private keys (dropped in the workspace root to pull
# store diagnostics). These are secrets - never commit them.
*.p8
