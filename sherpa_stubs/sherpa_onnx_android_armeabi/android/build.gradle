group = "com.k2fsa.sherpa.onnx.stub.armeabi"
version = "1.0"

buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.1.0'
    }
}

rootProject.allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

apply plugin: 'com.android.library'

android {
    namespace = "com.k2fsa.sherpa.onnx.stub.armeabi"
    compileSdk = 34

    defaultConfig {
        minSdk = 21
    }
}
